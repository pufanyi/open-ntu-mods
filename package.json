{
  "name": "open-ntu-mods",
  "private": true,
  "packageManager": "pnpm@11.5.0",
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "biome check .",
    "check:write": "biome check --write .",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "generate:api": "pnpm --filter frontend generate:api"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4"
  }
}
